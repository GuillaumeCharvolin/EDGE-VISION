## PROJECT GOAL

Run an object-detection model on each frame on video and measures how long each stage takes.

## SETUP

```
./edge-vision.sh        # build
./edge-vision.sh -r     # build & run
./edge-vision.sh -t     # build & tests
./edge-vision.sh -d     # build & docs
```